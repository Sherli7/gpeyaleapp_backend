# ===========================================
# Configuration GPE Yale Backend
# ===========================================
# Copiez ce fichier vers .env et modifiez les valeurs selon votre environnement

# ===========================================
# Configuration Serveur
# ===========================================
NODE_ENV=production
PORT=3003

# ===========================================
# Configuration Base de Données
# ===========================================
# Option 1: URL de connexion unique (recommandé)
DATABASE_URL=postgresql://postgres:${DB_PASSWORD}@host.docker.internal:5433/gpe_yale

# Option 2: Variables discrètes (si DATABASE_URL n'est pas défini)
# Configuration Production
DB_HOST=host.docker.internal
DB_PORT=5433

# Configuration Développement
DB_HOSTLOCAL=host.docker.internal
DB_PORTLOCAL=5433

# Informations d'authentification
DB_USERNAME=postgres
DB_PASSWORD=${DB_PASSWORD}
DB_NAME=gpe_yale

# ===========================================
# Configuration CORS
# ===========================================
# URL du frontend
FRONTEND_URL=https://gpe-yale.edocsflow.com

# Origines autorisées (séparées par des virgules)
CORS_ORIGINS=http://localhost:4200,http://localhost:3000,http://localhost:5173,https://gpe-yale.edocsflow.com,https://www.gpe-yale.edocsflow.com

# ===========================================
# Configuration SMTP (Email)
# ===========================================
# Serveur SMTP
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false

# Authentification SMTP
EMAIL_USER=${EMAIL_USER}
EMAIL_PASS=${EMAIL_PASS}

# Configuration des emails
EMAIL_FROM=${EMAIL_FROM}
EMAIL_BCC=admin@example.com

# Variables optionnelles pour les emails
EMAIL_INLINE_HEADER=true
EMAIL_HEADER_WIDTH=640
EMAIL_HEADER_QUALITY=72
EMAIL_HEADER_FORMAT=jpeg
EMAIL_VERIFY_ON_BOOT=false
EMAIL_DEBUG=false

# ===========================================
# Configuration SSL (Production uniquement)
# ===========================================
# Ces variables sont utilisées par Nginx en production
# SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
# SSL_KEY_PATH=/etc/nginx/ssl/key.pem

# ===========================================
# Configuration Logs
# ===========================================
LOG_LEVEL=info
LOG_FILE=/app/logs/app.log

# ===========================================
# Configuration Sécurité
# ===========================================
# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ===========================================
# Configuration Développement
# ===========================================
# Décommentez pour activer le mode debug
# DEBUG=true
# NODE_ENV=development
# FRONTEND_URL=http://localhost:4200
